import type { CandidateItem } from "./types";

// TODO: implement with twitterapi.io once TWITTERAPI_IO_KEY is available
export async function scrapeTwitter(
  _queries: string[],
  _keywords: string[],
): Promise<CandidateItem[]> {
  return [];
}
